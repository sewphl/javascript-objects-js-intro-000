
var playlist = {
  'bon jovi': "you give love a bad name"
};

function updatePlaylist(playlist, artistName, songTitle) {
  var p2 = Object.assign({playlist, { artistName: [songTitle] });
  return(p2);
}
